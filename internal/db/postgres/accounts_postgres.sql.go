// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: accounts_postgres.sql

package postgres

import (
	"context"
	"encoding/json"
)

const usernamesByAccountIDs = `-- name: UsernamesByAccountIDs :many

SELECT id, username
FROM accounts
WHERE id IN (
    SELECT value FROM jsonb_array_elements_text(CAST($1 AS jsonb))
)
ORDER BY id
`

type UsernamesByAccountIDsRow struct {
	ID       string
	Username string
}

// PostgreSQL account queries whose parameter syntax is engine-specific.
func (q *Queries) UsernamesByAccountIDs(ctx context.Context, accountIdsJson json.RawMessage) ([]UsernamesByAccountIDsRow, error) {
	rows, err := q.db.QueryContext(ctx, usernamesByAccountIDs, accountIdsJson)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []UsernamesByAccountIDsRow{}
	for rows.Next() {
		var i UsernamesByAccountIDsRow
		if err := rows.Scan(&i.ID, &i.Username); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
