# Optional. Leave unset to call the same origin that serves the SPA.
# In development the Vite proxy forwards /api, /livez, and /readyz to the Go
# backend on http://localhost:8080, so the same-origin default is correct.
# VITE_API_BASE_URL=http://localhost:8080/
VITE_ENABLE_MSW=false
